<UserControl
    x:Class="YTApp.UserControls.VideoViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YTApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <MediaPlayerElement x:Name="viewerAudio"/>
        <MediaElement x:Name="viewer" AreTransportControlsEnabled="True">
            <FlyoutBase.AttachedFlyout>
                <Flyout>
                    <StackPanel>
                        <MenuFlyoutItem Text="Download video"/>
                        <MenuFlyoutItem Text="Copy video link"/>
                        <MenuFlyoutItem Text="Copy video link at current time"/>
                    </StackPanel>
                </Flyout>
            </FlyoutBase.AttachedFlyout>
            <MediaElement.TransportControls>
                <local:CustomMediaTransportControls/>
            </MediaElement.TransportControls>
        </MediaElement>
    </Grid>
</UserControl>
