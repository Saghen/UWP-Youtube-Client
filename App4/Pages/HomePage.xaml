<Page
    x:Class="YTApp.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:YTApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:classes="using:YTApp.Classes"
    mc:Ignorable="d">

    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <TextBlock FontSize="18" Margin="0,20,20,20">Recoommended Videos</TextBlock>
        <GridView x:Name="VideoItemGridView" >
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="classes:YoutubeItemDataType">
                    <StackPanel Margin="10" Height="240" Width="280">
                        <Image x:Name="ImageControl" Source="{x:Bind Thumbnail}"/>
                        <TextBlock x:Name="TitleControl" Text="{x:Bind Title}" FontSize="15" Foreground="#fff" TextWrapping="Wrap" Height="40px" TextTrimming="CharacterEllipsis"/>
                        <TextBlock x:Name="AuthorControl" Text="{x:Bind Author}" FontSize="12" Foreground="#fff"/>
                        <TextBlock x:Name="OtherInfoControl" Text="{x:Bind ViewsAndDate}" FontSize="12" Foreground="#fff"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </StackPanel>
</Page>
